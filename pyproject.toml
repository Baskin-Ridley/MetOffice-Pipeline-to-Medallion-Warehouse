[project]
name = "noaa-pipeline-to-medallion-warehouse"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "apache-airflow>=3.2.1",
    "google-cloud-bigquery>=3.41.0",
    "google-cloud-storage>=3.10.1",
]
